[project]
name = "bli-core"
version = "0.1.0"
description = "bli SSOT: コマンド定義・プロトコルcodec・エラー（純Python・依存ゼロ）"
requires-python = ">=3.10"
dependencies = []  # ★依存ゼロを厳守（アドオンに vendoring するため）

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bli_core"]
